# Change Log

Running log of all moderate changes made during the research project. Each entry is dated and describes what changed, why, and whether it was effective. Intended as a reference for writing the paper's methods and results sections.

---

[2026-06-05] | [Phase 5 / VALIDATE verdict + thesis flip] | Analyzed the held-out validate run (3 tasks × 300 samples, disjoint skip=100, --scorer hybrid pinned; results/20260605_050905). DECISIVE, HYPOTHESIS-REJECTING RESULT: per-group/depth-aware budgeting does NOT beat uniform — best per-group is equal-or-worse than uniform in every (task × budget) cell at equal retained-token count; the few nominal "wins" are ≤+0.8% with fully overlapping 95% CIs (noise). RQ2 = No. Global flat-top-k also merely ties uniform → RQ3 = No (allocation strategy doesn't matter). MECHANISM (the real science): the independent 1D sweep's per-group effects (G2 fragile / G0 expendable) were real only because the other groups were held FULL; under joint pruning the separable water-filling prediction breaks because the depth-groups are MUTUALLY REDUNDANT — keeping a slice of every group (uniform) + a good feature-based scorer already captures the cross-group-overlapping info, while concentrating cuts (zeroing a group) loses unique coverage for no gain (e.g. TextVQA water-filled (0,0.8,0.7)=−2.1% vs uniform (.5,.5,.5)=−0.2%). POSITIVE HEADLINE: feature-based UNIFORM pruning is hugely compressible — General VQA keep 15%→−0.2%, TextVQA keep 30%→−0.3%, DocVQA keep 50%→+0.2% (held-out n=300, equal token count, bootstrap CIs); removes 50–85% of injected visual tokens at ≤~2% acc. DECISIONS (user, 2026-06-05): LOCK the analysis paper now (no more large runs); KEEP the failed per-group hypothesis as an honestly-documented negative result. DOCS: flipped paper.md §0 thesis (per-group budgeting → "feature-based uniform pruning is near-optimal; depth-aware allocation doesn't help, groups mutually redundant"); reordered contributions to (1) compressibility, (2) attention-fails, (3) depth-aware-allocation negative result; added §13 Phase 5 VALIDATE findings (head-to-head tables + verdict + mechanism); updated §5 RQ answers (RQ1 partial, RQ2/RQ3 No, RQ5 OCR>coarse), §15 Success Criteria outcomes, §16 Failure Modes ("uniform performs just as well" triggered — resolved by pivot, not collapse), §18 one-sentence summary, Current Status (Phase 5 complete; analysis direction locked). New working title: "How Compressible Is DeepStack? Feature-Based Token Pruning and the Limits of Depth-Aware Budgeting". No code/notebook/Colab changes. | yes — paper direction finalized; per-group method claim rejected and reframed as a compressibility analysis + negative result

[2026-06-04] | [Phase 5 / sweep results + validate prep] | Ran + deeply analyzed the full 3-task Stage-A SWEEP (general_vqa/docvqa/textvqa, n=100; results/20260604_232301). Key reads (recorded in paper.md §0 + §13 Phase 5 sweep findings): (1) VALIDITY — all 9 (task×group) r=0 endpoints reproduce Phase 3 drop_g{i} EXACTLY (e.g. TextVQA drop_G2=−4.0% both; DocVQA drop_G0=+1.0% both) → sweep machinery correct, curves trustworthy. (2) HEADLINE = extreme redundancy — can zero an ENTIRE DeepStack injection group (~33% of injected refinement) at ≤1% acc cost on general_vqa AND docvqa; with Phase 4b's 50%-uniform-free, DeepStack's added visual mass is heavily over-provisioned. (3) Clean per-group structure appears on TextVQA ONLY (G2 load-bearing −4.0%, G0 expendable/+1.3%); DocVQA SURPRISE — groups interchangeable (any one droppable ~free; only drop_all hurts), so per-group expected to TIE uniform there. → per-group-beats-uniform clean evidence is TextVQA-anchored. (4) Scorer unchanged: visualizer's accuracy-ranked best-scorer came out different per task (hybrid/random/vision_attention) = noise, proving accuracy can't rank scorers at n=100; KL gentlest for hybrid/magnitude, worst for random/vision_attention everywhere → pin hybrid. DECISIONS (user): paper headline = analysis-first (redundancy); next run = validate as-is on 3 tasks with aggressive targets. CODE: added `--scorer` to exp_budgeting validate (default `hybrid`, threaded run_validate→_validate_one_task→_validate_conditions) to STOP validate from building per-group budgets on budget.best_scorer's accuracy-noise auto-pick (it would have used random/vision_attention); "auto"/"none"/unknown → graceful fallback. colab_run.ipynb Cell 0 set to BUDGETING_MODE="validate", --num-samples 300, --targets 0.5,0.3,0.2,0.15, --scorer hybrid (auto-finds sweep dir). Local: py_compile/ruff/mypy clean; new CPU test (test_validate_scorer.py) asserts forced hybrid overrides the noisy auto-pick + fallbacks; budget tests pass; notebook JSON valid. | code+analysis — validate run pending; decides the per-group method claim

[2026-06-04] | [paper.md / direction] | Added a prominent §0 "Paper Direction — Thesis & Line of Reasoning" at the top of paper.md to make the paper's through-line visible (previously buried in the §13 phase logs). Thesis: which tokens to keep is feature-based not attention-based, and how many is depth/task-structured → feature-driven, task-aware, per-group token budget. Documents the 4-step reasoning arc (Structure: Phase 2 depth-ordered dispersion → Selection: Phase 4/4b both attention families fail, feature-based wins → Allocation: Phase 5 per-group budget is task-dependent, protect G2 on OCR, starve G0 → Realization: zeroing diagnoses, Stage B measures real latency), IS/IS-NOT framing (analysis-first + recipe, not a survey, not a systems paper), locked scope (3 tasks; one fixed feature-based scorer with the hybrid≈magnitude tie reported as robustness; estimate-now-measure-later efficiency), and the working title "Task-Aware Per-Depth Token Budgeting for DeepStack VLMs". Updated the now-stale "specialized to General VQA" framing in §13 Phase 5 + Current Status to the 3-task scope. | yes — clarifies the publishable direction; guides which metrics/analyses to emphasize

[2026-06-04] | [Phase 5 / multi-task] | Generalized exp_budgeting.py from General-VQA-only to a --tasks loop (default general_vqa,docvqa,textvqa), one result file per task (budgeting_sweep__<task>.json / budgeting_validation__<task>.json); validate now takes --sweep-dir (auto-finds the latest sweep dir). Notebook: budgeting dispatch passes BUDGETING_ARGS through; per-task visualize writes to figures/<task>/ so tasks don't overwrite each other; Cell 0 budgeting docs updated (3-task default, ~69k-gen full sweep on A100, --sweep-dir). Rationale (paper.md §0/§13 Phase 5): General VQA alone is compression-insensitive and cannot support the per-group-beats-uniform claim; DocVQA/TextVQA provide the OCR contrast (G2 load-bearing). Analyzed the 5-sample smoke run results/20260604_220546: pipeline validated end-to-end on GPU (all 4 scorers ran; vision_attention capture succeeded with zero N/A; KL captured; figures+EXPLAINER rendered+pushed). Accuracy saturated/quantized at 0.800 everywhere (n=5 artifact — ignore EXPLAINER's "best=random"); KL already shows depth-ordered sensitivity (fully zeroing a group: G2≈0.048 ≫ G1≈0.016 > G0≈0.008, ~6×), reproducing Phase 3. Local: py_compile/ruff/mypy clean; CPU tests pass. | code only — full 3-task sweep (num-samples 100, A100) + validate pending

[2026-06-04] | [Phase 5 / Stage A budgeting] | Implemented Stage A of per-group DeepStack budgeting, SPECIALIZED to General VQA (VQAv2) — the general/scalable target. Goal: find the optimal per-group keep-budget (r0,r1,r2) AND the optimal within-group scorer for general VQA via ZEROING (diagnostic; not real pruning). Settled the zeroing-vs-real-pruning question in paper.md §13 Phase 5: NOT mathematically the same — base visual tokens occupy sequence positions through every layer (masked_scatter @ modeling_qwen3_vl.py:1143) and DeepStack only ADDS refinements there (line 881); zeroing removes a group's additive contribution but leaves the token in the sequence (no latency change, but the only way to vary groups independently since all 3 share positions), whereas real pruning drops the base token (shorter seq, real latency, different logits, cannot be per-group). Stage A uses zeroing + equal-retained-token-count comparison; real sequence-shortening + latency is Stage B (deferred). New code: (1) src/deepstack/budget.py (pure logic, CPU-tested) — sweep_conditions (independent 1D per-group grid, r=0 deduped across scorers), retained_total, waterfill_budgets (exact separable search over the grid, ranked by predicted acc at the target retained count), uniform_budget, global_topk_keepsets (flat cross-group top-k for the RQ3 baseline), extract_curves/best_scorer. (2) prune.py: added DeepStackPruner.set_keep_indices (+ explicit-keepset branch in the hook) so global-top-k and precomputed joint keep-sets bypass scorer selection; backward-compatible. (3) src/experiments/_genutil.py: refactored generate/kl_first_token/num_visual_tokens/baseline_with_capture out of exp_scoring (no behavior change) so both experiments share identical metrics. (4) src/experiments/exp_budgeting.py: `sweep` (each group independently 100%->0% in 5% steps, all 4 scorers, VQA soft-acc + first-token KL, per-sample checkpointing, EmbedNormCapturer for the global baseline's per-token L2 scores) and `validate` (water-filled joint budgets vs uniform vs global-topk on a disjoint held-out split at equal retained-token count, bootstrap 95% CIs). (5) src/deepstack/visualize_budgeting.py: per-group sensitivity curves (acc + KL), scorer bars at the aggressive ratio, validation head-to-head bars, data-driven EXPLAINER. (6) colab_run.ipynb: RUN_BUDGETING toggle + BUDGETING_MODE/BUDGETING_ARGS (Cell 0), dispatch with auto-find of the latest sweep for validate + auto-visualize (Cell 6). Honest caveat recorded everywhere: general VQA is compression-INSENSITIVE (Phase 3/4b), so per-group may tie uniform — a reportable null; headroom + scorer choice still stand. Local verification: py_compile + ruff clean; mypy clean (--explicit-package-bases); CPU unit tests pass (sweep enumeration/dedup, retained_total, waterfill hits target + protects fragile group, global_topk exact-K + mapping + clamp, extract_curves/best_scorer, set_keep_indices zeroes complement + coexists with specs, visualizer renders all figures from synthetic JSON). | code only — Colab run pending (smoke at --num-samples 5, then sweep ~23k gens on A100, then validate)

[2026-06-04] | [Phase 4b / results] | Ran the accuracy-led 300-sample re-run on Colab (results/20260604_001256; methods random/activation_magnitude/hybrid/vision_attention; keep-ratios 1.0/0.50/0.25) and recorded findings in paper.md §13 Phase 4b. Verdict (ranked on accuracy; KL still magnitude-biased): (1) random clearly loses — DocVQA@0.25 collapses +6.0%; (2) HEADLINE: vision_attention does NOT win — competitive only at mild (0.50) pruning on text tasks, degrades to near-random at aggressive (0.25) pruning and on spatial/general tasks, beaten by both hybrid and magnitude on the clean DocVQA@0.25 (ordering hybrid≈magnitude>vision_attention>random); the pre-registered vision_attention≈magnitude guess is rejected. Attributed to (i) reading attention at intermediate DeepStack source layers 5/11/17 vs VisPruner's final layer, (ii) CLS-free attention-sink. (3) hybrid is the most robust scorer (wins DocVQA on acc+KL, never collapses); magnitude near-tied/simple fallback — resolves run-1's dead heat to hybrid≥magnitude. (4) 50% pruning ~free everywhere; headroom confirmed at scale. (5) task taxonomy firm (doc/text sensitive; general/counting insensitive). Both attention families now ruled out (decoder=Phase 2 null, vision=Phase 4b) → selection signal is feature-based; sharpens novelty vs "apply VisPruner". Decision: Phase 5 within-group scorer = hybrid (magnitude alternative; vision_attention demoted to ablation). | yes — settles the scorer choice; vision-attention negative result is a paper contribution

[2026-06-03] | [Phase 4b / vision attention] | Added the vision-encoder attention scorer and an accuracy-led re-run after run-1's verdict proved unsettled (magnitude vs hybrid a dead heat; KL biased toward magnitude). Confirmed from the literature (VisPruner ICCV2025 arXiv:2412.01818, FasterVLM) that VISION-encoder attention — not decoder attention — is the SOTA training-free signal and beats decoder-attention/random; VisPruner's CLS-free recipe ("average the rows of A = attention each patch receives") fits Qwen3-VL's ViT, which has no CLS/register tokens (verified). New `src/deepstack/saliency.py` (VisionAttentionCapturer): vision-only eager + wraps module-global eager_attention_forward to capture weights the model already computes (identity-matched to the 3 source-layer attn modules), maps pre→post-merge via received.reshape(N,4).mean(1) with a 4N guard, captured once during the eager baseline forward. `prune.py`: `vision_attention` score-based selector (top-k saliency; same exact-k + reconstruct-to-full-length contract) + DeepStackPruner.set_vision_scores. `exp_scoring.py`: defaults now {random, activation_magnitude, hybrid, vision_attention} (proven losers spatial/diversity dropped from default, still available), keep-ratios {1.0,0.50,0.25}, 300 samples/task, per-task checkpointing, vision_attention marked N/A on capture OOM. `visualize_scoring.py`: accuracy is the headline + KL-bias note; fixed a latent zigzag-line bug (x-values were non-monotonic). colab_run.ipynb SCORING_ARGS → 300. Corrected the overconfident §13 Phase 4 framing (added a correction banner; deferred the scorer decision to Phase 4b). Locally verified: py_compile/ruff/mypy clean; CPU tests for vision_attention selection, saliency collect mapping+guards, pruner hook wiring, exp helpers, and synthetic-JSON figure render all pass. | code only — Colab run pending

[2026-06-03] | [Phase 4 / results] | Ran the within-group scoring experiment on Colab (100 samples/task, 16 conditions, results/20260603_204751) and recorded findings in paper.md §13 Phase 4. Verdict (read via first-token KL — raw accuracy is noise at n=100, ~40% of cells show impossible negative drops): activation_magnitude wins overall (6/12 KL cells, best on DocVQA/counting), hybrid ties (5/12, best on TextVQA/general VQA), random/spatial_uniform/diversity lose — diversity NEVER wins and is sometimes worse than random (negative result that supersedes the Phase 1 "diversity matters most for G0" speculation). Cross-checked on DocVQA@0.25 (cleanest task): magnitude −0.8% vs random +3.9%. DeepStack additive refinement is highly compressible: 50–75% droppable with accuracy within noise (KL rises but answers rarely flip). Decision: Phase 5 fixes the within-group scorer to activation_magnitude (hybrid as ablation; drop diversity/spatial_uniform). Caveat: zeroing doesn't shorten the sequence, so no latency win yet — that's a later sequence-shortening + measurement step. | yes — establishes the scorer choice and the compressibility headroom for Phase 5

[2026-06-03] | [Phase 4 / pruning] | Implemented within-group visual-token pruning. `src/deepstack/prune.py`: five scorers (random, spatial_uniform, activation_magnitude, diversity=farthest-point sampling in a 32-d random projection, hybrid=magnitude-seeded magnitude+diversity greedy) — each returns exactly k=round(keep_ratio·N) unique kept indices so methods compare at an identical retained-token count. All implement the Phase-1 reconstruct-to-full-length contract (zero pruned rows, keep (N,D) shape) so DeepStack's 1:1 additive injection never breaks. `DeepStackPruner` is a non-invasive context manager (mirrors DeepStackAblator): pre-hook on Qwen3VLTextModel.forward prunes each group's embeds before injection; pre-hook on Qwen3VLModel.forward captures image_grid_thw for the spatial scorer. Greedy keeps all ops on-device (no per-step host sync). Verified on CPU: contract holds for all scorers at keep_ratio∈{1,.75,.5,.25}, exact-k, magnitude retains sink tokens, fallbacks for no-grid/multi-image. ruff+mypy clean. | code only — run pending on Colab

[2026-06-03] | [Phase 4 / experiment] | `src/experiments/exp_scoring.py` (Experiment 4): for each task × scorer × keep-ratio (uniform budget across all 3 groups, to isolate scorer quality from Phase-5 budget allocation), measures labeled accuracy (reuses Phase-3 VQA-soft/ANLS/count scorers + task registry) and first-token KL vs the no-pruning baseline. 16 conditions (1 baseline + 5 methods × 3 ratios); defaults 100 samples/task → 6,400 generations. Writes results/<ts>/scoring.json. `src/deepstack/visualize_scoring.py`: per-task accuracy-vs-keep-ratio curves, grouped bar chart at keep-ratio 0.50, and a data-driven EXPLAINER_scoring.md (verified on synthetic JSON). Wired into colab_run.ipynb (RUN_SCORING toggle + SCORING_ARGS in Cell 0; dispatch + auto-visualize in Cell 6). | code only — run pending on Colab

[2026-06-02] | [colab] | Fixed GITHUB_TOKEN loading in colab_run.ipynb Cell 0. Root cause: `userdata.get()` only resolves in the colab.research.google.com web UI; running via a connected kernel (VS Code/local Jupyter) raises TimeoutException ("Secrets can only be fetched when running from the Colab UI"), which the old bare `except Exception` swallowed as a misleading "No GITHUB_TOKEN secret found". Now the except surfaces the real exception type/message and falls back to a GITHUB_TOKEN environment variable, so the notebook works from the Colab UI (Secrets) or any other kernel (env var). | yes — token resolves in both run contexts; precise diagnostics on failure

## Format

```
[YYYY-MM-DD] | [COMPONENT] | [DESCRIPTION] | [EFFECTIVE?]
```

- **COMPONENT**: which part of the codebase or research (e.g., `profiling`, `pruning`, `budgeting`, `evaluation`, `model`, `experiment`)
- **DESCRIPTION**: what was changed and why
- **EFFECTIVE?**: yes / no / partial / pending — with a brief note on outcome if known

---

## Log

[2026-06-03] | [ablation] | Phase 3 sensitivity ablation RAN on Colab A100 (results/20260603_184741, Qwen3-VL-2B-Instruct, fp16, 100 samples/task, 4 tasks × 8 conditions). VERDICT: WEAK GO — task-dependent sensitivity confirmed. Key findings: (1) G2 (ViT L17, deep) is critical for OCR/text: TextVQA drop_g2=-4.0% accuracy (largest single-group drop in experiment), TextVQA keep_g0=-4.0% (same cost, confirming G0 cannot substitute G2 for text). (2) G0 (ViT L5, shallow) is expendable and slightly HARMFUL for detail tasks: drop_g0 improves TextVQA by +1.3% and DocVQA by +1.0% — consistent with Phase 2's CV=0.61 finding (many noisy tokens that slightly interfere with G1/G2 signal). G0 is the best candidate for aggressive pruning across all tasks. (3) Counting and general VQA are nearly immune to individual group ablations — all single-group drops ≤1.3%, only drop_all registers real decline (counting=-4%, docvqa=-5.3%, textvqa=-3.3%). (4) KL reveals G1 as the "bridging" group: lowest KL when dropped (0.020 — G0+G2 compensate well) but highest KL when kept alone (0.112 — G1's info is most distinct from what G0+G2 provide). (5) Several conditions beat full accuracy (drop_g0 for textvqa, all single-drops for docvqa) — likely sampling noise at n=100 (ANLS/VQA variance ≈±1-2% at this scale); directional findings are robust but absolute numbers need ≥500 samples to confirm. Actionable for Phase 4/5: prune G0 most aggressively (safe everywhere, helpful for OCR); protect G2 for text tasks; counting/general VQA tolerate aggressive uniform reduction. Phase 3 ablation mechanism (zeroing) is diagnostic only — it does not reduce sequence length; real efficiency requires input-level resolution reduction guided by this calibration table. | yes — go/no-go answered; task-dependent sensitivity confirmed for OCR; Phase 4/5 design now calibrated

[2026-06-03] | [paper-framing] | Locked the paper's central framing as the DISPERSION LENS. From the 8-real-image attention-on run (results/20260603_080941), the defining Phase 2 result is non-uniform WITHIN-GROUP feature dispersion: per-token L2-norm CV (std/mean) = 0.61/0.45/0.42 for G0/G1/G2, skew (max/median) 8.9×/4.3×/2.9× — shallow G0 is highly unequal/redundant (most prunable), deep G2 uniform/dense (most fragile). The hypothesis (§4) is now operationalized around CV (scale-free) rather than absolute norm, because absolute norm grows with depth as a residual-stream artifact (confound, explicitly noted). Dispersion is the signal that both motivates per-group budgeting AND parameterizes the allocation rule (bigger budgets → higher-dispersion groups). Two supporting (not headline) results kept: attention is an informative NULL (~1/seq-len floor, near-identical across groups — reproduces VisPruner → score on vision-side signals, not decoder attention), and DeepStack overhead is ~5 ms (so the win must come from token reduction). Documented in paper.md §4 (refined hypothesis + dispersion lens + caveat) and §13 Phase 2 Findings; checked the Phase 2 box. Hard caveat recorded everywhere: Phase 2 shows non-uniformity in feature STRUCTURE only — it motivates but does NOT confirm accuracy-sensitivity non-uniformity, which is the Phase 3 ablation's job. | yes — framing set; Phase 3 ablation is the validating experiment

[2026-06-03] | [profiling] | Phase 2 first results + visualization. First successful Colab instrument run (results/20260603_073821, 4 samples: 2 real + 2 synthetic, attention off) confirmed the core hypothesis signal: all 3 groups carry the SAME token count per image (DeepStack injects identical positions at every depth), but per-token feature strength (L2 norm) rises monotonically with depth — group 0 (ViT L5) mean 14.7 and heavily left-skewed (77% of tokens below the overall-median strength), group 1 (L11) mean 17.2 / 54%, group 2 (L17) mean 25.3 / only 15%. DeepStack extraction+injection overhead is tiny (~5 ms total), so speedups must come from cutting token count, not the injection op. Takeaway: shallow group 0 is by far the most prunable, deep group 2 the most fragile → direct support for per-group budgeting. To make this legible: (1) expanded REAL_CALIBRATION to 8 verified, task-diverse images (natural/counting/macro/scene/action/OCR/document/chart), all confirmed fetchable, so num-samples=8 is fully real with synthetic only as fallback; (2) added src/deepstack/visualize.py — reads a deepstack_instrument.json (no model load, runs locally) and writes results/<ts>/figures/ (norm-distribution overlay, mean-strength bars, prunability %, latency, attention overlay, one-page dashboard.png) plus a plain-English EXPLAINER.md; (3) colab_run.ipynb now sets num-samples=8 + INSTRUMENT_CAPTURE_ATTENTION=True and auto-runs visualize after each instrument run so figures get pushed. Verified locally: ruff/mypy/py_compile clean, figures render correctly on the existing JSON. | yes — figures generated; awaiting the 8-real-image + attention-on Colab re-run for paper-grade versions

[2026-06-03] | [profiling] | Phase 2 hardening — first Colab instrument run died with exit code 1 and no Python traceback (hallmark of a hard CUDA-OOM kill, not an exception). Root cause: unvetted/uncapped calibration image URLs — a giant image inflates the prefill token count and OOMs the T4. Fixes in instrument.py: (1) fetch + size-cap every image locally to ≤1024px longest side (no OOM from huge images); (2) per-sample try/except — a failed fetch falls back to a deterministic synthetic image, a failed forward is skipped, run only aborts if ALL samples fail; (3) curated reliable URLs (Qwen demo + COCO cats), SSL-verify fallback for missing local cert stores, and a --synthetic-only fully-offline mode; (4) prefill-only seq-length guards on the injection hooks; (5) flushed per-sample diagnostics + per_sample_source in the JSON. Added INSTRUMENT_SYNTHETIC_ONLY toggle to colab_run.ipynb. Verified locally: ruff/mypy/py_compile clean, plan-building/synthetic-gen/size-cap/dead-URL-fallback/distribution-math all unit-exercised, both real URLs fetch + cap correctly. | yes — run can no longer be killed by a bad/oversized image; awaiting Colab re-run

[2026-06-03] | [profiling] | Phase 2 started — added src/deepstack/instrument.py: a non-invasive DeepStackInstrumentor (context manager) that attaches forward hooks to measure, across a small calibration set, the five quantities Phase 2 calls for: (1) per-group token count, (2) per-group feature-norm distribution (stats + percentiles + 30-bin histogram, pooled over tokens×samples), (3) opt-in per-group visual-token attention saliency (--capture-attention, forces eager attention, hooks only Qwen3VLTextAttention injection layers via module.layer_idx), (4) extraction latency (pre/post hooks on each deepstack PatchMerger) and injection latency (bracketing decoder layers 0..N — injection i runs between layer i return and layer i+1 call), (5) per-group GPU memory deltas + peak. Group count / vision layers / out_hidden_size are read from the loaded model config at runtime, so it self-corrects to the actual [5,11,17]/2048 found in Phase 1 rather than the class defaults. Reuses Qwen3VLEvaluator/create_default_test_case and the probe's hook/context-manager pattern; runs one prefill forward per sample over a built-in CALIBRATION_IMAGES list (natural/OCR/chart/counting). Writes results/<ts>/deepstack_instrument.json. CLI mirrors probe.py (+ --num-samples, --capture-attention). Wired into colab_run.ipynb (RUN_INSTRUMENT toggle + INSTRUMENT_ARGS/INSTRUMENT_CAPTURE_ATTENTION in Cell 0, dispatch branch in Cell 6). Local checks pass (py_compile, ruff, mypy, notebook JSON). | pending — awaiting Colab GPU run to populate the distributions

[2026-06-03] | [profiling] | Phase 1 probe RAN on Colab T4 (results/20260603_050848/deepstack_probe.json). Key runtime findings on Qwen3-VL-2B-Instruct: (1) actual config taps vision layers [5,11,17] — NOT the class default [8,16,24]; always read deepstack_visual_indexes from the loaded model. (2) out_hidden_size=2048 = text hidden dim (groups pre-projected into LLM space). (3) demo image grid [1,86,128] → 2752 tokens/group, 3 groups; count_match PASS all, grid cross-check MATCH. (4) mutation test confirmed the 1:1 count-contract: naive 25% drop raised ValueError (2752 vs 2064), reconstruct-to-full PASSED. (5) Per-group token L2-norm grows with depth (μ 15.1→17.9→23.2); group 0 (layer 5) is heavy-tailed (max 141.7 ≈ 9× mean) → activation-magnitude scoring needs a diversity term there. Corrected layer/dim numbers in paper.md §13 Phase 1 and added a Phase 1 Runtime Results subsection with implications for Phases 2/4/5. | yes — Phase 1 fully closed, runtime-confirmed

[2026-06-02] | [profiling] | Phase 1 complete — mapped Qwen3-VL DeepStack internals. Added src/deepstack/probe.py: a non-invasive forward-hook probe (DeepStackProbe) that reuses Qwen3VLEvaluator for loading + create_default_test_case for input, captures the 3 deepstack groups (vision layers [8,16,24]), per-group shape/dtype/L2-norm stats, injection depths (decoder layers 0/1/2), the visual_pos_masks count, and cross-checks the visual-token count against image_grid_thw. Includes a mutation test proving the strict 1:1 count-contract: naively dropping tokens from a group breaks the injection add (shape mismatch), while prune-then-reconstruct-to-full-length (scatter kept tokens, zero-fill dropped) keeps it valid — this dictates the Phase 4 pruning design. Writes results/<ts>/deepstack_probe.json. Documented findings in paper.md §13 Phase 1 and checked the Phase 1 box. | yes — internals verified against source; runtime confirmation pending the Colab probe run

[2026-06-02] | [workflow] | Enforced hard rule: the model is NEVER run locally — all model runs (evaluate.py, probe.py, future experiments) go through the Colab GPU via colab_run.ipynb. Documented in CLAUDE.md (Colab GPU Workflow). Wired the probe into colab_run.ipynb: Cell 0 gains RUN_PROBE toggle + PROBE_ARGS; Cell 6 dispatches to `python -m src.deepstack.probe` when RUN_PROBE=True, else evaluate.py. | yes — one-click probe run on Colab

[2026-06-01] | [project-setup] | Created CLAUDE.md (operating instructions), paper.md (full research context), and log.md (this file). Established project structure for DeepStack-Aware Visual Token Budgeting research. | yes — foundational setup

[2026-06-01] | [codebase-cleanup] | Deleted src/__pycache__, scripts/setup_branch_protection.py, docs/compile.bat. Created empty package scaffolding: src/deepstack/ (probe, ablation, prune, budget) and src/experiments/ (exp_sensitivity, exp_budgeting, exp_scoring, exp_latency, exp_pareto). Added root requirements.txt and results/README.md documenting 9 baseline runs. | yes — clean starting point for Phase 1

[2026-06-01] | [codebase-cleanup-2] | Deleted all 9 old results/ run directories, docs/ folder, .cursorrules, CODEOWNERS. Full clean slate. | yes

[2026-06-01] | [environment] | Created Python 3.13 venv (.venv/). Installed PyTorch 2.12 (MPS/M1 support), all requirements. Patched local_transformers/dependency_versions_table.py to remove stale huggingface-hub<1.0 upper bound (installed version is 1.17.0). Verified local_transformers wiring — Qwen3VLForConditionalGeneration resolves to local source. Downloaded Qwen3-VL-2B-Instruct (4.0 GB, safetensors). Fixed torch_dtype→dtype deprecation in evaluate.py. Created start.sh and setup.md. Smoke test (CPU, float32, 32 tokens) running — local source confirmed in output. | yes — full environment ready

[2026-06-02] | [colab] | Rebuilt colab_run.ipynb as a one-click GPU runner: Cell 0 config (model path, eval args, reads GITHUB_TOKEN from Colab Secrets), then mount Drive → clone/pull repo → install deps → download model to Drive (cached) → verify GPU + local_transformers wiring → run evaluate.py → commit & push results/ back to GitHub. Token-bearing git URLs are never printed (sanitized subprocess wrapper). Documented the GitHub<->Colab<->local data flow in CLAUDE.md (new "Colab GPU Workflow" section). Model weights stay in Drive, code + results round-trip via GitHub, traces gitignored. | yes — single Run-all executes a full eval and returns results locally on git pull

[2026-06-01] | [evaluate.py] | Renamed Qwen3VLTesting.py → evaluate.py. Fixed two bugs: (1) local_transformers wiring was broken — Qwen3VLForConditionalGeneration was not registered in local_transformers/__init__.py, so the sys.modules redirect was silently falling through to installed transformers; fixed by importing directly from local_transformers.models.qwen3_vl.modeling_qwen3_vl. (2) device_map branch never called from_pretrained, leaving self.model as None. Added startup print showing exact source file of model class for verification. | yes — local model edits now reflected at runtime

---
